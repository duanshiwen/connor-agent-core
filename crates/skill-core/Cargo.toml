[package]
name = "skill-core"
version = "0.1.0"
edition = "2024"
description = "Skill manifest, registry, and validation contracts for AgentOS runtime skills."
license = "Apache-2.0"

[dependencies]
action-core = { path = "../action-core" }
model-adapter = { path = "../model-adapter" }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
thiserror = { workspace = true }
